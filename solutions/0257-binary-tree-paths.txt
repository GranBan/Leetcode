            str = "";
        } else {
            helper(root->left, str);
            helper(root->right, str);
            str += "->";
            tree_res.push_back(str);
        if (root->left == nullptr && root->right == nullptr) {
        str += std::to_string(root->val);
    void helper(TreeNode* root, std::string str) {

        if (root == nullptr) return;
