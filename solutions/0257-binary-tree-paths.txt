    void helper(TreeNode* root, std::string str) {

        if (root == nullptr) return;
    }
        return tree_res;
        helper(root, "");
        if (root == nullptr) return tree_res;
    vector<string> binaryTreePaths(TreeNode* root) {
public:
    std::vector<std::string> tree_res;
private:
