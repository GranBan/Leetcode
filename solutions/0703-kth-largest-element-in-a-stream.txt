    }
    
    int add(int val) {
        pq.push(val);
        if (pq.size() > k) {
            pq.pop();
        }
        }
            pq.pop();
        while (pq.size() > k) {
    }
        }
        return pq.top();
