class Solution {
public:
    int missingNumber(vector<int>& nums) {
        int n = nums.size();
        int sum_n = n * (n+1) / 2;
        int sum_nums = 0;

        for (int n : nums) {
            sum_nums += n;
        }
        return sum_n - sum_nums;
    }
};