        return diameter;
        depth(root);
        if (root == nullptr) return 0;
    }
    int depth(TreeNode* root) {
    int diameterOfBinaryTree(TreeNode* root) {
public:
    int diameter = 0;
private:
class Solution {
 */
