            tree_res.push_back(temp);
        } else {
            target -= root->val;
            pathSumBuilder(root->left, temp, target);
            pathSumBuilder(root->right, temp, target);
            target += root->val;
        }
        temp.pop_back();
    }
};
